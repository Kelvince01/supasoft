# Development Environment Configuration

# Show SQL queries in console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Detailed logging for development
logging.level.com.supasoft=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.cache=DEBUG
logging.level.org.springframework.data.redis=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Disable caching for development (optional)
# spring.cache.type=none

# H2 Console (if needed for testing)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Development-specific database (if different)
# spring.datasource.url=jdbc:mysql://localhost:3306/supasoft_items_dev?useSSL=false&serverTimezone=Africa/Nairobi
